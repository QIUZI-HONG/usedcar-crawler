# 给面试官看：二手车行情数据采集项目（一页纸说明）

> 面向非技术面试官。3 分钟看完，知道这个项目**做了什么、能给公司带来什么、怎么亲眼验证**。
> 技术细节在文末「可能被追问的问题」里，可按需展开。

---

## 一、这个项目解决什么问题（30 秒版本）

二手车公司每天要做两件事：**定价**和**决定收什么车**。做这两件事需要知道"市场上同款车现在卖多少钱、跌了多少"。
过去靠人工去网站一页页翻、用 Excel 手抄，慢、容易错、还经常漏。

**我做的这套系统：每天自动把公开的车源信息抓下来 → 整理成干净表格 → 呈现在一个看板上。**
人只需要打开看板，就能看到：现在有多少车在卖、什么价、哪款车保值、哪个城市车源多。

**当前库里是 103 条真实在售车源**（新能源 30 / 燃油 73，覆盖 45 个品牌、39 个城市），
全部来自瓜子二手车公开放行的车源详情通道——**没有一条是模拟或编造的数据**。

---

## 二、先看结果：打开这个看板

📊 **`dashboard/index.html`** —— 双击用浏览器打开即可，**不需要联网、不需要装任何东西**。

看板上能看到：

| 看板模块 | 业务价值 |
|---|---|
| 顶部 5 个数字卡片 | 车源总量、平均售价、平均保值率、覆盖城市、覆盖品牌——一眼看大盘 |
| 新能源 / 燃油 大盘对比 | 两条业务线谁更保值、价差多少、车源量对比 |
| **新能源看板** | 保值率 TOP 车型、品牌均价、**续航 × 价格散点图**、电池类型与电池健康度分布 |
| **燃油看板** | 价格区间分布、**车龄 × 里程热力图**、品牌均价排行、排放标准与变速箱构成 |
| 车龄 vs 保值率曲线 | 车越老越不值钱，这条曲线用来判断"这个报价合不合理" |
| **车源明细表**（可按品牌/城市/燃油类型筛选，可搜索） | 就是交付给业务同学的"干净表"，每一行都能点回原车页面核对 |
| 字段完整率 | 数据可信度自检：哪些字段源站没给，一目了然，**不装作数据完美** |
| 采集任务运行记录 | 每次跑了多久、抓到多少、新增/更新/拒收各多少——可追溯 |

看板顶部还会标注**数据来源、采集引擎、生成时间**，让看数据的人知道这份数据是什么时候、从哪来的。

---

## 三、岗位要求对照表（逐条对应，附现场可验证的证据）

| 岗位要求 | 我的实现 | 证据（可当场演示） |
|---|---|---|
| 对公开网站进行数据采集 | 站点通过配置文件管理，新增站点不改代码；详情页源走站点地图自动发现 | `config/sites.yaml` + `crawl` 命令 |
| 编写/维护爬虫，定时批量抓取 | 每天凌晨自动跑增量、每周全量校准；支持断点续爬 | `schedule` 命令 + 任务记录表 |
| 处理反爬 | 三档取数（普通 → 隐身 → 浏览器渲染）；**识别"HTTP 200 假页面"**；命中风控自动冷却、超限熔断，不硬刚 | `crawl` 的冷却/熔断日志，`docs/DATA_SOURCE.md` |
| 处理页面结构变化 | 提取规则全在配置里；**内容契约校验**：页面对不上结构直接判为无效，不让脏页混进统计 | `probe` 命令可看每个字段命中数 |
| 清洗、去重、字段标准化 | 价格统一万元、里程统一公里、上牌时间拆年/月、品牌归一；同一辆车重复出现只更新不新增 | 明细表 + 复跑后总量不变 |
| 输出 Excel / CSV / 数据库表 | 一键导出 Excel（自带"字段说明"页）和 CSV，同时入库 | `exports/` 目录下 4 个真实文件 |
| 搭建采集文档（字段说明、更新频率、异常记录） | 数据字典 + 运维手册 + 数据源合规审计 + 每次采集的日志表 | `docs/` 下 5 份文档 |
| 响应临时取数需求 | 命令行一键查统计 / 日志 / 合规状态 / 导出任意业务线 | `stats`、`logs`、`compliance`、`export` |
| Python 能力 | 全项目 Python，分层清晰（取数 / 解析 / 加工 / 存储 / 展示） | 源码 |
| 加分：Playwright | 已集成，用于 JS 动态渲染页面 | `fetcher.py` 的 `DynamicFetcher` 档位 |
| 加分：SQL | SQLAlchemy Core 双兼容（SQLite 开发 / MySQL 生产），唯一键 UPSERT | `storage/repository.py` |
| 加分：可视化 | 上述看板（11 张图表，单文件 HTML，离线可用） | `dashboard/index.html` |
| 加分：GitHub 作品 | 完整工程：源码 + 配置 + 5 份文档 + **229 个自动化测试** | 项目目录 |

---

## 四、现场演示：3 条命令

```bash
# ① 离线自检：解析 / 清洗 / 去重 / 幂等入库 / 导出 全链路跑一遍（零网络）
python -m usedcar_crawler selftest

# ② 真实采集：站点地图 -> 逐条车源详情页
python -m usedcar_crawler crawl --line all --maps 2 --sample 10

# ③ 生成看板（数据已在库内，无需联网）
python -m usedcar_crawler viz
```

刷新 `dashboard/index.html` 即可看到本次采集的增量。
另外两条随时可跑的核验命令：

```bash
python -m usedcar_crawler compliance                     # 合规巡检：各源 robots 结论
python tools/verify_against_raw.py                        # 溯源对账：库内值 vs 原始页面逐条比对
```

---

## 五、数据从哪来（重要，我会主动说明）

| 站点 | robots.txt 实测结论 | 处置 |
|---|---|---|
| **瓜子二手车** | 明确 **放行** `/car-detail/*.md` 与 `/*.md$`，并提供了约 14 万条车源的站点地图索引 | **采用**：只走这条被显式放行的机器可读通道 |
| 懂车帝 / 汽车之家二手车 | 明确写了 `Disallow: /`（**全站禁止**） | **直接禁用并留档**，一条请求都不发 |
| 人人车 / 第一车网 | 验证码拦截 / 纯前端渲染 | 不采用 |

> 这一点我会主动讲：**爬虫工程师的价值不是突破封锁，而是稳定、合规、低成本地拿到干净数据。**

**还有一个我会主动交代的真实情况**：采集密度升高后，目标站点开始对详情页通道
返回"HTTP 200 但内容是空壳页"的软风控（不是 403，也不是验证码）。
我做了三件事——① 用**内容契约校验**把这种页识别出来，绝不当成"正常但解析不出字段"的页面；
② **冷却 + 熔断**，达到上限就收手，不硬刚；③ **raw 层与解析层解耦**，
已经落盘的原始响应可以用 `replay` 命令离线重算、重新入库。

所以本项目交付的 103 条数据是**真实抓取**的响应；在出口 IP 未被限流的环境下，
`crawl` 命令可直接实时抓取，代码路径完全一致。

---

## 六、工程质量（技术面试官会看的部分）

| 项目 | 数字 |
|---|---|
| 自动化测试 | **229 个用例，全部通过**（全部离线可跑） |
| 已入库真实数据 | **103 条车源**（新能源 30 / 燃油 73，45 品牌 / 39 城市） |
| 溯源对账 | **103/103 条**，库内值与原始页面逐字一致，0 处不符 |
| 核心字段覆盖 | 价格 / 品牌 / 里程 / 新车指导价 / 保值率 / 城市 **100%** |
| 重复运行 | 数据总量不变（幂等），不会产生重复行 |
| 车源下架追踪 | 连续 3 次抓不到才标记下架，重新出现自动恢复 |
| 脏数据拦截 | 价格"面议"这类无效数据被拒收并留痕，不进库 |
| 续航口径红线 | 增程/插混车的"综合续航"**绝不**当成纯电续航入库，宁可留空 |

---

## 七、可能被追问的问题（提前准备好答案）

**Q1：为什么不用 requests + BeautifulSoup？**
用了一个更强的库 Scrapling，它一套同时覆盖"普通请求 / 隐身模式 / 浏览器渲染"三档，
比自己拼 requests + Playwright 更省事，也更容易维护。解析思路一样（都是按 CSS 选择器取字段）。

**Q2：网站改版了怎么办？**
提取规则全部在配置文件里，改版只改配置 + 一条命令校准；同时有"字段缺失率告警"、
"内容契约校验"和"原始响应留存"，出问题能立刻定位、不用重新抓一遍。

**Q3：会不会被对方网站封？**
有三道保护：先查对方是否允许抓取（一票否决）；按源独立限速；被拦就冷却、超限熔断而不是硬重试。
全程只采公开信息，不碰登录、不破解验证码、不采集个人信息。

**Q4：数据量能放大吗？**
架构上留好了位置：加并发采集（换 Scrapling 的 Spider 框架）、加代理池、去重换 Redis、
断点续爬换分布式。当前逻辑与存储层不需要重写。

**Q5：为什么看板不是实时刷新的网页？**
当前是单文件静态看板（数据内嵌，双击即开，方便演示与离线查看）。
要变成实时网页，加一个只读 API 接口即可，采集与清洗部分不用动。

**Q6：你怎么保证数据是真的？**
两个可当场执行的证明：
① `python tools/verify_against_raw.py` —— 用一套**独立于解析器**的正则直接从原始响应里抠数字，
与库内干净值逐条对账，103 条零差异；
② 看板明细表每一行都带原车页面链接，点开即可人工核对。

---

## 八、一句话总结

**这套系统的价值不在于"能爬"，而在于"合规地爬 + 把脏数据变成别人能直接用的干净表 +
出问题时能说清数据从哪来、为什么不一致"。**

---

*完整技术验收结论见 `docs/验收测试报告.md`；数据源合规审计见 `docs/DATA_SOURCE.md`。*
